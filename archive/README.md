# Archived inference source

`frozen_inference_source.zip` is a deterministic, read-only provenance snapshot
of the original inference-era source tree. Verify it with the adjacent
`frozen_inference_source.zip.sha256` file.

Do not install or edit this archive for reporting work. The active canonical
source is the workspace-root `src/`, `tests/`, `scripts/`, and `configs/` tree.
